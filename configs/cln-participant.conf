# CLN participant node — regtest
# Mounted read-only at /etc/cln-participant.conf in cln-p{0,1,2} containers.

network=regtest

# Bitcoin Core backend
bitcoin-rpcuser=user
bitcoin-rpcpassword=password
bitcoin-rpcconnect=bitcoind
bitcoin-rpcport=18443

# hold plugin (BoltzExchange/hold, binary installed in Dockerfile.cln)
plugin=/usr/local/bin/hold

# Listen for peer connections
addr=0.0.0.0:9735

log-level=info
